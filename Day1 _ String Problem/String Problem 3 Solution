#include <bits/stdc++.h>
using namespace std;
int main() {
	string s;
	cin>>s;

	string s1;
	for(int i=s.size()-1;i>=0;i--)
	{
		s1.push_back(s[i]);
	}
	

	if(s == s1)
	{
		cout<<"YES";
	}
	else{
		cout<<"NO";
	}
}
